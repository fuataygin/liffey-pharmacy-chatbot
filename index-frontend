<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title>Liffey Pharmacy AI Assistant</title>

  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: Arial, Helvetica, sans-serif;
      background: #f4f7f6;
      color: #1f2933;
    }

    .header {
      background: #087f5b;
      color: white;
      padding: 24px 20px;
      text-align: center;
    }

    .header h1 {
      margin: 0 0 8px;
      font-size: 26px;
    }

    .header p {
      margin: 0;
      font-size: 14px;
      opacity: 0.95;
    }

    .container {
      max-width: 850px;
      margin: 30px auto;
      padding: 0 16px;
    }

    .chat-card {
      background: white;
      border-radius: 12px;
      box-shadow: 0 3px 15px rgba(0,0,0,0.08);
      overflow: hidden;
    }

    .chat {
      height: 500px;
      overflow-y: auto;
      padding: 20px;
    }

    .message {
      margin-bottom: 16px;
      display: flex;
    }

    .message.user {
      justify-content: flex-end;
    }

    .bubble {
      max-width: 78%;
      padding: 12px 15px;
      border-radius: 12px;
      line-height: 1.5;
      white-space: pre-wrap;
    }

    .assistant .bubble {
      background: #eef7f3;
      border: 1px solid #d7eee5;
    }

    .user .bubble {
      background: #087f5b;
      color: white;
    }

    .input-area {
      border-top: 1px solid #e5e7eb;
      padding: 15px;
      display: flex;
      gap: 10px;
    }

    #messageInput {
      flex: 1;
      border: 1px solid #cbd5e1;
      border-radius: 8px;
      padding: 12px;
      font-size: 15px;
      outline: none;
    }

    #messageInput:focus {
      border-color: #087f5b;
    }

    button {
      border: none;
      background: #087f5b;
      color: white;
      padding: 0 22px;
      border-radius: 8px;
      font-size: 15px;
      cursor: pointer;
    }

    button:hover {
      background: #066b4d;
    }

    button:disabled {
      background: #94a3b8;
      cursor: not-allowed;
    }

    .disclosure {
      margin-top: 18px;
      padding: 14px;
      background: #fff8e1;
      border: 1px solid #f0df9b;
      border-radius: 8px;
      font-size: 13px;
      line-height: 1.5;
    }

    .loading {
      color: #64748b;
      font-style: italic;
    }

    @media (max-width: 600px) {
      .chat {
        height: 60vh;
      }

      .input-area {
        flex-direction: column;
      }

      button {
        height: 44px;
      }

      .bubble {
        max-width: 90%;
      }
    }
  </style>
</head>

<body>

  <header class="header">
    <h1>Liffey Pharmacy AI Assistant</h1>
    <p>Ask about our products, prices, availability, offers and general medicine information.</p>
  </header>

  <main class="container">

    <div class="chat-card">

      <div id="chat" class="chat">

        <div class="message assistant">
          <div class="bubble">
            Hello! I'm the Liffey Pharmacy AI Assistant. I can help with products, prices, stock, special offers, pharmacy services and general medicine information.
          </div>
        </div>

      </div>

      <div class="input-area">
        <input
          id="messageInput"
          type="text"
          placeholder="Ask a pharmacy-related question..."
          autocomplete="off"
        >

        <button id="sendButton">
          Send
        </button>
      </div>

    </div>

    <div class="disclosure">
      <strong>AI disclosure:</strong>
      This is an AI-powered pharmacy assistant and may make mistakes.
      Product, price and availability information is retrieved from the pharmacy's live catalogue, but important information should be confirmed with a member of the pharmacy team.
      RxNorm information is provided as public reference information and is not a substitute for advice from a pharmacist or doctor.
    </div>

  </main>

  <script>

    const BACKEND_URL =
      "https://script.google.com/macros/s/AKfycbyErparIvcM8NU2IU9q3rPABEqibVSW9GgTrrTQjRtBJMsl1Mm2AQHaBl2pkTmoUDRLdw/exec";

    const chat = document.getElementById("chat");
    const input = document.getElementById("messageInput");
    const sendButton = document.getElementById("sendButton");

    function addMessage(text, type) {

      const message = document.createElement("div");
      message.className = "message " + type;

      const bubble = document.createElement("div");
      bubble.className = "bubble";

      bubble.textContent = text;

      message.appendChild(bubble);
      chat.appendChild(message);

      chat.scrollTop = chat.scrollHeight;

      return message;
    }

    async function sendMessage() {

      const message = input.value.trim();

      if (!message) {
        return;
      }

      addMessage(message, "user");

      input.value = "";
      input.disabled = true;
      sendButton.disabled = true;

      const loading = addMessage("Thinking...", "assistant");
      loading.querySelector(".bubble").classList.add("loading");

      try {

        const response = await fetch(BACKEND_URL, {
          method: "POST",
          headers: {
            "Content-Type": "text/plain;charset=utf-8"
          },
          body: JSON.stringify({
            message: message
          })
        });

        if (!response.ok) {
          throw new Error("Server returned HTTP " + response.status);
        }

        const data = await response.json();

        loading.remove();

        if (data.error) {
          addMessage(
            "Sorry, I couldn't process that request. Please try again.",
            "assistant"
          );
        } else {
          addMessage(
            data.answer || "I couldn't produce an answer.",
            "assistant"
          );
        }

      } catch (error) {

        console.error(error);

        loading.remove();

        addMessage(
          "I'm sorry, I couldn't connect to the pharmacy assistant. Please try again.",
          "assistant"
        );

      } finally {

        input.disabled = false;
        sendButton.disabled = false;
        input.focus();

      }
    }

    sendButton.addEventListener("click", sendMessage);

    input.addEventListener("keydown", function(event) {

      if (event.key === "Enter") {
        sendMessage();
      }

    });

  </script>

</body>
</html>
